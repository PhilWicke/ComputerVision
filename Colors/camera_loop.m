function [image] = camera_loop(url, varargin)
%CAMERA_LOOP Creates a webcam loop while further processing it
%   The camera loop is terminated when pressing a button
%   Inputs:     url = snapshot address
%               varargin:       'align' - align for eye tracking
%    

%%CORRUPTED
   
    
    close(fig);
end
